Install dependencies:
1. pip install -r requirements.txt

Configure API Key:
1. Open .env_template and replace YOUR_API_KEY with your actual API key.
2. After adding the key, save the file and rename it to .env by removing _template from the filename.

Running the Application:
1.A python app.py
1.B Alternatively, set up a shortcut in the project folder to run this command.